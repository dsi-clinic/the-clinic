## Handlers

Just have one function that does everything in order and returns things one by one...avoid weird looping logic. Use try except blocks so it's clear if something is breaking.

## What Else?